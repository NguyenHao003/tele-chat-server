services:
  app:
    build: .
    container_name: tele-chat-server
    restart: always
    ports:
      - '3000:3000'
    env_file:
      - .env
    extra_hosts:
      - 'host.docker.internal:host-gateway'
